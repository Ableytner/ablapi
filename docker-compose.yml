services:
  api:
    container_name: ablapi
    hostname: ablapi
    build:
      context: .
#    image: ghcr.io/ableytner/ablapi:dev
    ports:
      - 9219:8000
    environment:
      MODULES: "gtnh,hello_world"
      GITHUB_TOKEN: ${GITHUB_TOKEN}
    restart: unless-stopped
